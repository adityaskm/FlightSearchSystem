import { LayoverFlightDurationPipe } from './layover-flight-duration.pipe';

describe('LayoverFlightDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new LayoverFlightDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
