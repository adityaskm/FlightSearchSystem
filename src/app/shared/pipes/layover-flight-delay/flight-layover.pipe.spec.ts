import { FlightLayoverPipe } from './flight-layover.pipe';

describe('FlightLayoverPipe', () => {
  it('create an instance', () => {
    const pipe = new FlightLayoverPipe();
    expect(pipe).toBeTruthy();
  });
});
